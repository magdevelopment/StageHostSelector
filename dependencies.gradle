ext {
    // Build Plugins
    androidBuildPluginVersion = '3.2.1'

    // Language
    kotlinVersion = '1.3.0'

    // Android Support
    supportVersion = '28.0.0'
    constraintLayoutVersion = '1.1.3'

    // Network
    okhttpVersion = '3.11.0'
}